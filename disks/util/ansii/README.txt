# ansii.prg

A small program to change colors to ANSII terminal colors.  You can run this before using the Novaterm 80 column terminal driver to install the proper ANSII colors. Temporary solution until the Novaterm driver can do this itself.
