# Set this info to the output you want to use to test modes
# Also set DEFAULT_MODE to whatever your current mode is so the
# to_default.sh script can reset back after switching to one
# of the custom modes
export OUTPUT=HDMI-1
export DEFAULT_MODE=1920x1080
